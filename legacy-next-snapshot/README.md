# Legacy Next.js Snapshot (Inactive Reference Archive)

This directory is an inactive snapshot of the original Next.js implementation of GH Store.
It is preserved as a historical and behavioral reference only.

- The active storefront and admin application lives in `storefront/` running **React 19 + React Router v8 on Cloudflare Workers**.
- No code in this directory is executed, bundled, or deployed in production.
- Do not add new features or modifications here; all active development happens inside `storefront/`.
